import random


class Dragon:
    def __init__(self, name, x_pos=50, y_pos=50):
        self.name = name
        self.scene = Sce
        self.x_pos = random.randint(0, scene.x_pos)
        self.y_pos = random.randint(0, scene.y_pos)


    def __str__(self):
        return self.name
